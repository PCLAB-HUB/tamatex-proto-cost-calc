---
name: 依存ライブラリ追加と .gitignore 更新
status: closed
created: 2026-04-21T01:54:31Z
updated: 2026-04-21T02:30:00Z
github: https://github.com/PCLAB-HUB/tamatex-proto-cost-calc/issues/2
depends_on: []
parallel: false
conflicts_with: []
---

# Task: 依存ライブラリ追加と .gitignore 更新

## Description

新UI実装に必要なライブラリ3点を `requirements.txt` に追加し、シナリオDBファイルを Git 追跡対象から除外する設定を `.gitignore` に追加する。後続タスクのすべてがこのタスクの完了を前提とするため、**エピック最初に単独で実行**する。

追加後、`pip install -r requirements.txt` がローカルで成功すること、Streamlit Cloud のビルドが通ることをこのタスクで確認する。

## Acceptance Criteria

- [ ] `requirements.txt` に以下の3行が追加されている:
  - `streamlit-aggrid>=0.3.4`
  - `plotly>=5.20`
  - `streamlit-extras>=0.4`
- [ ] `.gitignore` に以下のエントリが追加されている:
  - `proto/data/scenarios.db`
  - `proto/data/*.db-journal`
- [ ] ローカルで `pip install -r requirements.txt` が成功する（`.venv` 使用）
- [ ] ローカルで `streamlit run proto/app.py` を起動でき、既存アプリが従来通り動作する（import エラーが起きない）
- [ ] Streamlit Cloud デプロイ（`PCLAB-HUB/tamatex-proto-cost-calc` の main ブランチに push してビルドログを確認）が成功する
  - ※ この検証は feature/proto-cost-calc でのタスク完了後、デプロイ前の最終統合タスク(010)で改めて実施する。このタスクではローカル検証までで良い

## Technical Details

### requirements.txt への追加

既存の `requirements.txt`（65行）末尾に以下を追加:

```
streamlit-aggrid>=0.3.4
plotly>=5.20
streamlit-extras>=0.4
```

バージョンピン留め理由:
- `streamlit-aggrid>=0.3.4`: カラムフォーマッタ API が安定した最小版
- `plotly>=5.20`: Python 3.11 互換性と Streamlit との統合が安定
- `streamlit-extras>=0.4`: `metric_cards` API が安定

### .gitignore への追加

`.gitignore` 末尾に以下セクションを追加:

```
# Scenario persistence (per-user local DB)
proto/data/scenarios.db
proto/data/*.db-journal
```

### 動作確認手順

```bash
cd /Users/pclab/Desktop/Project/tamatex/.worktrees/proto-cost-calc
source .venv/bin/activate  # ない場合は python -m venv .venv
pip install -r requirements.txt
streamlit run proto/app.py
# ブラウザで http://localhost:8501 を開き、既存UIが表示されることを確認
```

## Dependencies

- **前提**: なし（エピック最初のタスク）
- **後続ブロッカー**: 002, 003, 004, 005 のすべてがこのタスク完了を待つ

## Effort Estimate

- Size: XS
- Hours: 0.5

## Definition of Done

- [ ] requirements.txt 変更をコミット（メッセージ: `Issue #<N>: 依存ライブラリ追加（aggrid/plotly/streamlit-extras）`）
- [ ] .gitignore 変更を同一コミットに含める
- [ ] ローカルでアプリが既存通り起動することを確認
- [ ] コードレビュー（セルフレビュー可）
