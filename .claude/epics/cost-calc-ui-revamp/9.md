---
name: サイドバー改修（保存ボタン・折りたたみ整理）
status: open
created: 2026-04-21T01:54:31Z
updated: 2026-04-21T02:19:25Z
github: https://github.com/PCLAB-HUB/tamatex-proto-cost-calc/issues/9
depends_on: [3]
parallel: true
conflicts_with: []
---

# Task: サイドバー改修（保存ボタン・折りたたみ整理）

## Description

既存 `proto/ui/sidebar.py`（192行）を改修して、以下を実現する:

1. **シナリオ保存ボタン** をサイドバー下部に追加
2. **輸入経費11項目×2**（計22 widget）をデフォルト折りたたみに変更
3. **最頻使用項目**（コンテナ条件・為替・マージン）を上段に整理
4. **シナリオ復元用ヘルパ関数** をエクスポート（タスク007が利用）

既存の widget key 命名規約（`{kp}_{field}`）は**絶対に変えない**。変えるとシナリオ復元や session_state 持続性が壊れる。

## Acceptance Criteria

### 改修ファイル
- [ ] `proto/ui/sidebar.py` — 既存を編集

### 新規エクスポート
- [ ] `apply_condition_to_session_state(cond: ImportCondition) -> None` — サイドバーの全 widget key に対応する session_state を一括設定（シナリオ読み込み時に呼ぶ）

### 画面構成（改修後の上から順）

1. **コンテナ条件** (radio) — 現状維持
2. **💱 為替レート** (社内為替 + 現行為替) — 現状維持
3. **🎯 マージン・ロス率** (マージン + ロス率 + 資材ロット + 資材ロス率) — 現状維持
4. **📦 輸入パラメータ** (海外運賃 + 保険率 + 関税率) — 現状維持
5. **🚚 物流** (入出庫 + 保管料 + ヶ月) — 現状維持
6. **💰 輸入経費（上級者設定）** — **デフォルト折りたたみに変更**
   - [ ] expander で囲む（`expanded=False`）
   - [ ] 単品用・ギフト用の2つの expander
7. **💾 シナリオ保存** (新規)
   - [ ] テキスト入力（シナリオ名）
   - [ ] 保存ボタン
   - [ ] 成功/エラーメッセージ

### 機能要件
- [ ] 既存の `render_sidebar()` の戻り値 `ImportCondition` の内容・型は一切変えない
- [ ] widget key 命名規約を維持: `{kp}_{field_name}` 形式
- [ ] コンテナ切替時のキー再生成ロジックも維持
- [ ] 保存ボタンはクリック時に `st.session_state["_save_requested"]` = (name, cond) をセットし、シナリオタブで実際の保存処理を行うか、またはサイドバー側で `repo.save_scenario()` を直接呼ぶ（後者推奨、シンプル）

### シナリオ復元 (apply_condition_to_session_state)
- [ ] 引数 `cond` の全フィールドを対応する widget key に書き込み
- [ ] ネストされた `ImportExpenses` も展開して書き込み
- [ ] 対応する widget key がまだ存在しない場合でも、session_state に事前セットすることで次回 render_sidebar() で使われる
- [ ] コンテナ（`cond.name`）が `_CONDITIONS` に存在しない場合は `ValueError` 送出

## Technical Details

### 保存ボタンの実装方針

**案A（推奨）**: サイドバーで `repo` を引数に受け取って直接保存

```python
def render_sidebar(repo: ScenarioRepository | None = None) -> ImportCondition:
    # ... 既存処理 ...

    # --- シナリオ保存セクション ---
    if repo is not None:
        st.sidebar.divider()
        st.sidebar.subheader("💾 シナリオ保存")
        scenario_name = st.sidebar.text_input(
            "名前",
            placeholder="例: USD150_20FT_標準",
            key="sidebar_scenario_name",
        )
        if st.sidebar.button("現在の条件を保存", type="primary", use_container_width=True):
            if not scenario_name.strip():
                st.sidebar.error("名前を入力してください")
            else:
                try:
                    sid = repo.save_scenario(scenario_name.strip(), condition)
                    st.sidebar.success(f"✅ 保存しました (id={sid})")
                except ScenarioNameConflictError:
                    st.sidebar.error(f"この名前は既に使われています")

    return condition
```

- `render_sidebar()` の既存呼び出し箇所は `repo` が None のまま渡されるため互換性維持
- タスク010 で `app.py` から `repo` を渡すように統合

### 輸入経費を折りたたみデフォルトに

既存の `_edit_import_expenses()` は既に `st.expander(label, expanded=False)` で囲まれているが、実体のサイドバー展開ロジックを見直して:

```python
# 変更前
with st.sidebar:
    exp_single = _edit_import_expenses(...)
    exp_gift = _edit_import_expenses(...)
```

現状が既に折りたたみになっていれば、この項目はラベル変更・「（上級者設定）」の注記追加だけで足りる。実際のコードを確認して判断すること。

### apply_condition_to_session_state の実装

```python
def apply_condition_to_session_state(cond: ImportCondition) -> None:
    """サイドバーの widget key に cond の値を書き込み、次回描画で反映させる."""
    if cond.name not in _CONDITIONS:
        raise ValueError(f"Unknown condition name: {cond.name}")

    kp = cond.name.replace(" ", "_").replace("/", "_")
    st.session_state[f"{kp}_internal_rate"] = cond.internal_rate
    st.session_state[f"{kp}_current_rate"] = cond.current_rate
    st.session_state[f"{kp}_margin_pct"] = cond.margin_pct
    st.session_state[f"{kp}_loss_rate_pct"] = cond.loss_rate_pct
    st.session_state[f"{kp}_material_lot"] = cond.material_lot
    st.session_state[f"{kp}_material_loss_pct"] = cond.material_loss_pct
    st.session_state[f"{kp}_freight"] = cond.overseas_freight_usd
    st.session_state[f"{kp}_insurance"] = cond.insurance_rate
    st.session_state[f"{kp}_tariff"] = cond.tariff_rate
    st.session_state[f"{kp}_io_fee"] = cond.io_fee
    st.session_state[f"{kp}_storage_fee"] = cond.storage_fee
    st.session_state[f"{kp}_storage_months"] = cond.storage_months

    # 輸入経費（単品）
    for field, val in _expenses_fields(cond.import_expenses_single, f"{kp}_exp_s").items():
        st.session_state[field] = val
    # 輸入経費（ギフト）
    for field, val in _expenses_fields(cond.import_expenses_gift, f"{kp}_exp_g").items():
        st.session_state[field] = val

    # コンテナ条件 radio
    st.session_state["sidebar_container_radio"] = cond.name  # ラジオボタンの key を固定化する必要あり


def _expenses_fields(exp: ImportExpenses, prefix: str) -> dict[str, float]:
    return {
        f"{prefix}_cic_usd": exp.cic_usd,
        f"{prefix}_cy": exp.cy_charge,
        f"{prefix}_thc": exp.thc,
        f"{prefix}_emc": exp.emc,
        f"{prefix}_cic2": exp.cic2,
        f"{prefix}_do": exp.do_fee,
        f"{prefix}_doc": exp.doc_fee,
        f"{prefix}_customs": exp.customs_fee,
        f"{prefix}_handling": exp.handling_fee,
        f"{prefix}_drayage": exp.drayage,
        f"{prefix}_devanning": exp.devanning,
    }
```

### コンテナ選択 radio にキーを付与
既存:
```python
selected = st.sidebar.radio("コンテナ条件", list(_CONDITIONS.keys()), horizontal=True)
```
を:
```python
selected = st.sidebar.radio(
    "コンテナ条件",
    list(_CONDITIONS.keys()),
    horizontal=True,
    key="sidebar_container_radio",
)
```
に変更（復元時に選択状態を制御するため）。

## Dependencies

- **前提**: 002（`ScenarioRepository` が存在すること）
- **並行作業**: 007（シナリオタブ）は本タスクの `apply_condition_to_session_state` を使うため、この API を先に確定させる
- **後続依存**: 010（app.py 統合）

## Effort Estimate

- Size: S-M
- Hours: 3

## Definition of Done

- [ ] sidebar.py 改修完了、`render_sidebar()` 既存呼び出し箇所の互換性維持
- [ ] `apply_condition_to_session_state` を `__all__` に追加（明示エクスポート）
- [ ] 手動動作確認: 保存ボタン → DBに保存される（一時的に app.py にprint文等で確認）
- [ ] 手動動作確認: `apply_condition_to_session_state` 呼び出し → サイドバーの値が全て復元される
- [ ] 既存 131 テスト PASS
- [ ] コミット: `Issue #<N>: サイドバー改修（保存ボタン・折りたたみ・復元関数）`
