# Task Plan: QNAP NAS Excel → Google Spreadsheet 自動同期システム (tamatex)

## Goal
QNAP NAS上のExcelファイル（10〜20個）を15分間隔で監視し、変更検知時にGoogle Spreadsheetへ自動反映するPythonスクリプトを構築する。事務員のExcel運用は一切変更せず、営業・役員が外出先からスプレッドシートで閲覧可能にする。

## Current Phase
Phase 7（テスト & 品質保証進行中）

## システム概要図

```
[QNAP NAS]                [常時稼働PC]                    [Google Cloud]
 ┌─────────┐   SMB/CIFS    ┌──────────────────┐   API      ┌─────────────────┐
 │ Excel    │◄─────────────►│ tamatex          │──────────►│ Google Sheets   │
 │ ファイル  │  (ネットワーク  │ ├─ watcher       │           │ (営業・役員閲覧) │
 │ 10-20個  │   ドライブ)    │ ├─ excel_reader  │           │                 │
 │          │               │ ├─ sheets_sync   │           │ Google Drive    │
 └─────────┘               │ ├─ state (SQLite)│           │ (同期フォルダ)   │
      ▲                    │ └─ scheduler     │           └─────────────────┘
      │                    └──────────────────┘
  事務員が編集                 15分ごとにポーリング
```

## Phases

### Phase 1: プロジェクト基盤構築
- [x] プロジェクトディレクトリ構造の作成
- [x] pyproject.toml / requirements.txt 作成
- [x] .gitignore 作成（シークレット除外）
- [x] config.example.yaml テンプレート作成
- [x] git init & 初回コミット
- **Status:** done

### Phase 2: 設定管理 & 状態管理モジュール
- [x] config.py — dataclass(frozen=True)による設定読み込み・バリデーション（※Pydanticから変更）
- [x] state.py — SQLiteによる同期状態管理（ファイルハッシュ、最終同期日時等）
- [x] logger.py — 構造化ログ設定（ファイル出力 + ローテーション）
- [x] ユニットテスト作成（test_config.py: 21件, test_state.py: 16件, test_logger.py: 17件）
- **Status:** done

### Phase 3: ファイル監視モジュール
- [x] watcher.py — NASフォルダのポーリング監視
- [x] ファイル変更検知（mtime + MD5ハッシュ比重検知）
- [x] テンポラリファイル除外（~$*, *.tmp, .~lock*）
- [x] 新規ファイル / 更新ファイル / 削除ファイルの分類
- [x] NAS接続断のグレースフル処理
- [x] ユニットテスト作成（test_watcher.py: 30件）
- **Status:** done

### Phase 4: Excel読み取りモジュール
- [x] excel_reader.py — openpyxlによるExcelデータ抽出
- [x] 複数シート対応
- [x] マージセル対応（値展開）— read_only=Falseでmerged_cells.ranges使用
- [x] データ型変換（日付、数値、文字列、空セル）
- [x] 日本語ファイル名・セル内容のUTF-8処理
- [x] data_only=Trueで計算結果値取得（read_only=Falseに変更）
- [x] ユニットテスト作成（test_excel_reader.py: 23件）
- **Status:** done

### Phase 5: Google Sheets同期モジュール
- [x] sheets_sync.py — Google Sheets API連携
- [x] サービスアカウント認証（gspread.Client(auth=creds)に移行済み）
- [x] スプレッドシート新規作成（folder_id直接指定に修正済み）
- [x] 既存スプレッドシートへのデータ上書き更新（URL固定化）
- [x] シート名の同期（Excel側のシート構成変更に追従、削除ロジック修正済み）
- [x] バッチ更新（API呼び出し最適化）
- [x] Google Driveフォルダへの整理
- [x] 共有設定（営業・役員への自動共有）
- [x] APIレート制限対応
- [ ] ユニットテスト作成（Google API依存のため統合テストで対応予定）
- **Status:** done

### Phase 6: メインスケジューラ & 統合
- [x] main.py — エントリーポイント
- [x] threading.Event + time.sleepによる15分間隔ポーリング（※APSchedulerから変更）
- [x] 同期フロー統合（検知 → 読取 → 同期）
- [x] エラーハンドリング統合（ファイル単位のスキップ & 継続）
- [x] グレースフルシャットダウン（SIGTERM/SIGINT対応、threading.Event使用）
- [ ] ヘルスチェック（ハートビートファイル出力）
- [ ] 統合テスト作成
- **Status:** done

### Phase 7: テスト & 品質保証
- [x] 全ユニットテスト実行・通過確認（107件 ALL PASSED）
- [ ] 統合テスト（モックAPI使用）
- [ ] エッジケーステスト
  - [x] NAS切断中の動作（detect_changes_skips_deletion_when_current_files_is_empty）
  - [ ] ファイルロック中の動作
  - [ ] 空ファイル / 破損ファイル
  - [ ] 巨大ファイル（10,000行超）
  - [ ] 同時更新（複数ファイルが同時に変更）
- [x] セキュリティチェック（シークレット漏洩なし確認済み）
- **Status:** in_progress

### Phase 8: デプロイメント & 運用準備
- [x] Windowsサービス化（GUIインストーラーに統合）
- [x] 初回セットアップスクリプト（scripts/initial_setup.py）
- [x] 運用手順書（docs/導入手順書.md: 607行）
- [x] 顧客向け導入マニュアル（docs/システム仕様書.md + PDF）
- [x] GUIインストーラーウィザード（scripts/installer.py: tkinter 7ステップ）
- **Status:** done

## Key Questions

1. NASのマウントパスは？（例: `Z:\共有\Excel` or `\\192.168.x.x\shared`）→ **未確認**
2. Excelファイルの形式は .xlsx のみか、.xls も含むか？→ **未確認（.xlsx前提で進める）**
3. 常時稼働PCのOSは Windows か？→ **未確認（Windows前提で進める）**
4. 閲覧者のGoogleアカウントのドメインは？→ **未確認**
5. Excelにマクロ（VBA）は使われているか？→ **未確認（データ中心のため無い前提）**
6. スプレッドシートの閲覧権限は全員同一か、ファイルごとに異なるか？→ **未確認**

## Decisions Made

| Decision | Rationale |
|----------|-----------|
| ポーリング方式（watchdog不使用） | SMB/CIFSネットワークドライブではファイルシステムイベントが信頼できない。15分間隔ポーリングが最も安定 |
| openpyxl使用 | .xlsx読み取りのデファクトスタンダード。data_only=Trueで計算結果値を取得可能 |
| gspread使用 | Google Sheets APIの高水準ラッパー。google-api-python-clientより簡潔 |
| サービスアカウント認証 | OAuth同意フロー不要。サーバー常駐型に最適。ドメイン全体委任も可能 |
| SQLite状態管理 | 外部DB不要。ACID準拠。ファイル1つで完結。10-20ファイル程度の管理に十分 |
| クリア＆全書き込み方式 | 差分更新より単純で確実。データ中心のためセル書式保持不要。API呼び出し回数も許容範囲 |
| ~~Pydantic設定管理~~ dataclass(frozen=True) | 外部依存を減らすためdataclassに変更。frozen=Trueで不変性を保証 |
| ~~APScheduler使用~~ threading.Event + sleep | 外部依存を減らし、シャットダウン即応性を向上（Event.waitで即座に解除） |
| NSSM (Windows Service) | Pythonスクリプトを簡単にWindowsサービス化。自動再起動・ログ統合 |
| MD5 + mtime二重検知 | mtimeだけではNAS環境で不正確な場合がある。ハッシュ併用で変更見逃しを防止 |

## Errors Encountered
| Error | Attempt | Resolution |
|-------|---------|------------|
| (未発生) | - | - |

## Notes
- 同期は **一方向**（NAS → Spreadsheet）。スプレッドシート側の編集は想定しない
- スプレッドシートのURLは固定（営業がブックマーク可能）
- 1ファイルの処理に5-10秒として、20ファイルでも最大200秒（3分強）。15分間隔に十分収まる
- Google Sheets API制限: 300 req/min/project, 60 req/min/user — 20ファイルなら余裕
- 日本語ファイル名を正しく扱うためUTF-8を徹底
